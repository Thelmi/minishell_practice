ls|pwd|""     pointer freed wasn't allocated
export PATH=      zsh: illegal hardware instruction  ./minishell (if something is already there it should replace value)
> " "     minishell(39209,0x110304dc0) malloc: *** set a breakpoint in malloc_error_break to debug
ls>a>b     should only create a (if it has content it will be deleted using O_TRUNC) and redirects in last file (b in this case)
<< ">"    whatever is after the heredoc should be a delimiter and not redirect in/out or whatever

